import { Box } from "@mui/material";
import { styled } from "@mui/system";

const middleinsert = style(Box)({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
})


export default middleinsert;